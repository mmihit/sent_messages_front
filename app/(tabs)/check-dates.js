import { Text, View } from "react-native";

export default function Check_dates() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text> check dates page </Text>
        </View>
    )
}